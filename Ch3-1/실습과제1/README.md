# 구조체와 클래스의 차이점을 설명하라
- 기본 접근 지정자 publice, private의 차이와 사용목적 구조체는 데이터 저장용 클래스는 데이터 + 기능 통합용이다.

---
  
# 클래스와 객체의 차이점을 설명하라.
- 클래스는 객체를 만들기 위한 방법
- 객체는 클래스를 바탕으로 메모리에 저장된 실제 데이터 단위

---

# sizeof(클래스명)의 결과값을 출력해보고 결과를 설명하라.
<img width="841" height="389" alt="image" src="https://github.com/user-attachments/assets/dbf1b76f-0d11-4224-b46d-bbaf6798a1ef" />
- 클래스가 메모리에 차지하는 바이트수를 확인이 가능하고, 결과 값은 16바이트이다.
