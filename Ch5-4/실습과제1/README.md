# 실습과제1

### 일반생성자와 복사생성자의 차이는?
- 객체를 처음 만들 때 멤버 변수들을 초기화하는 함수이며, 이미 존재하는 객체를 이용해서 새로운 객체를 만들 때 호출된다.
### 대입연산과 얕은 복사생성자의 차이는 무엇인가?
- 대입연산은 이미 만든어진 값에 덮어 쓰는 것이고 얇은 복사생성자는 객체를 새로 생성하면서 주소값만 복사하는 방식이다. 
### 지금까지 배운 내용기준으로 컴파일러가 자동으로 추가하는 멤버함수는 어떤 것이 있는가?(4가지)
- 1.기본생성자 2.복사생성자 3. 생성자 4. 복사 대입 연산자
